server:
  port: 8081

spring:
  application:
    name: stock-data
  datasource:
    url: jdbc:mysql://localhost:3306/find_stock?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      dialect: org.hibernate.dialect.MySQLDialect